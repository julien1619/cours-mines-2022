type Props = {
  message?: string;
  threadRootId?: string;
  originalId?: string;
};

export default function PostTweet({
  message,
  threadRootId,
  originalId,
}: Props) {
  return <div>PostTweet</div>;
}
